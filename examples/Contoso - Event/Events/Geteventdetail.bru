meta {
  name: Geteventdetail
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/events/{{eventId}}
  body: none
  auth: inherit
}

params:query {
  ~includeDetails: {{PLACEHOLDER}}
}

vars:pre-request {
  eventId: {{PLACEHOLDER}}
}

settings {
  encodeUrl: true
  timeout: 0
}

